import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2.57.4";
import { createRemoteJWKSet, importPKCS8, jwtVerify, SignJWT } from "npm:jose@5.10.0";
const FIREBASE_PROJECT_ID="mic-rhema";
const FIREBASE_ISSUER=`https://securetoken.google.com/${FIREBASE_PROJECT_ID}`;
const FIREBASE_JWKS=createRemoteJWKSet(new URL("https://www.googleapis.com/service_accounts/v1/jwk/securetoken@system.gserviceaccount.com"));
const GOOGLE_TOKEN_URL="https://oauth2.googleapis.com/token",FIRESTORE_SCOPE="https://www.googleapis.com/auth/datastore";
const CURRENT_PUBLISHABLE_KEY="sb_publishable_Dv98hBnbJB2TzRCG6aJNwA_KMPHLZSw";
const QUIZ_CATALOG_VERSION="quiz-v1-20260905",QUIZ_CATALOG_COMMIT="b058141542821fc97e1de64bdd5dbf912ef904a3";
const SOURCE_URLS=[`https://raw.githubusercontent.com/bichocutela/Mic-RHEMA-Ia-Studio-Google/${QUIZ_CATALOG_COMMIT}/app/src/main/java/com/aistudio/micrhema/BibleQuizCatalog.kt`,`https://raw.githubusercontent.com/bichocutela/Mic-RHEMA-Ia-Studio-Google/${QUIZ_CATALOG_COMMIT}/app/src/main/java/com/aistudio/micrhema/BibleQuizExpansion.kt`];
const LEVEL_8_PLUS=new Set(["semente_da_fe","caminho_da_promessa","escudo_da_fe","aguas_vivas","videira_verdadeira","luz_do_mundo","armadura_de_deus","leao_de_juda","chama_do_espirito","coroa_da_vida","asas_da_promessa","tabernaculo","arca_da_alianca","nova_jerusalem","gloria_eterna"]);
type ServiceAccount={project_id?:string;client_email?:string;private_key?:string};type FirestoreValue={stringValue?:string;booleanValue?:boolean;integerValue?:string;doubleValue?:number;timestampValue?:string;arrayValue?:{values?:FirestoreValue[]};mapValue?:{fields?:Record<string,FirestoreValue>};nullValue?:null};type FirestoreDocument={fields?:Record<string,FirestoreValue>};type Difficulty="easy"|"medium"|"hard";type Activity="quiz_easy"|"quiz_medium"|"quiz_hard";type QuizQuestion={id:string;difficulty:Difficulty;activity:Activity;prompt:string;options:string[];correctOptionIndex:number;hardHint:string;easyHint:string;reference:string;explanation:string};type MemberAuth={memberId:string;xpUnlocked:boolean;legacyXp:number;legacyQuizIds:string[];firebaseAuth:boolean};
let catalogCache:Map<string,QuizQuestion>|null=null;const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, apikey, content-type, x-client-info","Access-Control-Allow-Methods":"POST, OPTIONS","Content-Type":"application/json; charset=utf-8","Cache-Control":"no-store"};const json=(body:Record<string,unknown>,status=200)=>new Response(JSON.stringify(body),{status,headers:cors});const clean=(v:unknown,max=1000)=>String(v??"").trim().slice(0,max);const normalizePhone=(v:unknown)=>{const d=String(v??"").replace(/\D/g,"");return d.length>=12&&d.length<=13&&d.startsWith("55")?d.slice(2):d};
function fromValue(v?:FirestoreValue):unknown{if(!v)return null;if(v.stringValue!==undefined)return v.stringValue;if(v.booleanValue!==undefined)return v.booleanValue;if(v.integerValue!==undefined)return Number(v.integerValue);if(v.doubleValue!==undefined)return v.doubleValue;if(v.timestampValue!==undefined)return v.timestampValue;if(v.arrayValue!==undefined)return(v.arrayValue.values??[]).map(fromValue);if(v.mapValue!==undefined)return Object.fromEntries(Object.entries(v.mapValue.fields??{}).map(([k,x])=>[k,fromValue(x)]));return null}function docData(doc?:FirestoreDocument|null){return Object.fromEntries(Object.entries(doc?.fields??{}).map(([k,v])=>[k,fromValue(v)]))}const stringList=(v:unknown)=>Array.isArray(v)?v.map(String).filter(Boolean):[];function activityMap(v:unknown):Record<string,string[]>{if(!v||typeof v!=="object"||Array.isArray(v))return{};return Object.fromEntries(Object.entries(v as Record<string,unknown>).map(([k,x])=>[k,stringList(x)]))}function parseLegacyXp(e:string){const n=Number(e.slice(e.lastIndexOf("=")+1));return Number.isFinite(n)&&n>0?Math.floor(n):0}function parseLegacyQuizIds(entries:string[]){return[...new Set(entries.map(e=>{const cut=e.lastIndexOf("=");const r=(cut>0?e.slice(0,cut):e).trim();return r.startsWith("quiz:")?r.slice(5).trim():""}).filter(Boolean))]}
async function googleToken(a:ServiceAccount){if(!a.client_email||!a.private_key)throw new Error("Credencial Firebase incompleta.");const now=Math.floor(Date.now()/1000);const assertion=await new SignJWT({iss:a.client_email,scope:FIRESTORE_SCOPE,aud:GOOGLE_TOKEN_URL}).setProtectedHeader({alg:"RS256",typ:"JWT"}).setIssuedAt(now).setExpirationTime(now+3600).sign(await importPKCS8(a.private_key.replace(/\\n/g,"\n"),"RS256"));const r=await fetch(GOOGLE_TOKEN_URL,{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded"},body:new URLSearchParams({grant_type:"urn:ietf:params:oauth2:grant-type:jwt-bearer",assertion})});const b=await r.json();if(!r.ok||!b.access_token)throw new Error("Falha ao autenticar no Firebase.");return String(b.access_token)}async function getDocument(projectId:string,token:string,collection:string,id:string):Promise<FirestoreDocument|null>{const r=await fetch(`https://firestore.googleapis.com/v1/projects/${projectId}/databases/(default)/documents/${collection}/${encodeURIComponent(id)}`,{headers:{Authorization:`Bearer ${token}`}});if(r.status===404)return null;if(!r.ok)throw new Error(`Falha ao consultar ${collection}.`);return await r.json()as FirestoreDocument}
function difficultyFromToken(v:string):Difficulty|null{const x=v.toLowerCase();return x.includes("easy")?"easy":x.includes("medium")?"medium":x.includes("hard")?"hard":null}function activityFor(d:Difficulty):Activity{return d==="easy"?"quiz_easy":d==="medium"?"quiz_medium":"quiz_hard"}function baseXp(d:Difficulty){return d==="easy"?10:d==="medium"?20:30}function splitTopLevel(value:string){const out:string[]=[];let current="",depth=0,quoted=false,escaped=false;for(const ch of value){if(escaped){current+=ch;escaped=false;continue}if(ch==="\\"&&quoted){current+=ch;escaped=true;continue}if(ch==='"'){quoted=!quoted;current+=ch;continue}if(!quoted){if(ch==="("||ch==="[")depth++;else if(ch===")"||ch==="]")depth--;if(ch===","&&depth===0){out.push(current.trim());current="";continue}}current+=ch}if(current.trim())out.push(current.trim());return out}function unquote(value:string){const t=value.trim();if(!t.startsWith('"')||!t.endsWith('"'))return t;try{return JSON.parse(t)}catch{return t.slice(1,-1).replace(/\\"/g,'"').replace(/\\n/g,'\n').replace(/\\\\/g,'\\')}}function parseOptions(v:string){const s=v.indexOf("listOf("),e=v.lastIndexOf(")");if(s<0||e<=s)return[];return splitTopLevel(v.slice(s+7,e)).map(unquote)}function parseCatalog(source:string,target:Map<string,QuizQuestion>){for(const raw of source.split(/\r?\n/)){const line=raw.trim();if(!line.includes("quizQuestion("))continue;const s=line.indexOf("quizQuestion(")+13,e=line.lastIndexOf(")");if(e<=s)continue;const a=splitTopLevel(line.slice(s,e));if(a.length<9)continue;const id=unquote(a[0]),d=difficultyFromToken(a[1]),opts=parseOptions(a[3]),correct=Number(a[4]);if(!id||!d||opts.length!==4||!Number.isInteger(correct)||correct<0||correct>3)continue;target.set(id,{id,difficulty:d,activity:activityFor(d),prompt:unquote(a[2]),options:opts,correctOptionIndex:correct,hardHint:unquote(a[5]),easyHint:unquote(a[6]),reference:unquote(a[7]),explanation:unquote(a[8])})}}function parseExpansion(source:string,target:Map<string,QuizQuestion>){for(const raw of source.split(/\r?\n/)){const line=raw.trim();if(!/^(easy|medium|hard)_\d+\|/.test(line))continue;const p=line.split("|");if(p.length<12)continue;const d=p[0].startsWith("easy_")?"easy":p[0].startsWith("medium_")?"medium":"hard";const opts=[p[2],p[3],p[4],p[5]],correct=Number(p[6]),reference=p[7];if(!Number.isInteger(correct)||correct<0||correct>3)continue;target.set(p[0],{id:p[0],difficulty:d,activity:activityFor(d),prompt:p[1],options:opts,correctOptionIndex:correct,hardHint:`Procure o detalhe no contexto de ${reference}.`,easyHint:`A resposta direta é: ${opts[correct]}.`,reference,explanation:`${opts[correct]} é a resposta indicada em ${reference}.`})}}async function catalog(){if(catalogCache)return catalogCache;const target=new Map<string,QuizQuestion>();for(const url of SOURCE_URLS){const r=await fetch(url,{headers:{"User-Agent":"MIC-Rhema-Quiz-Authority"}});if(!r.ok)throw new Error(`Catálogo oficial indisponível (${r.status}).`);const source=await r.text();if(url.includes("Catalog"))parseCatalog(source,target);else parseExpansion(source,target)}if(target.size!==300)throw new Error(`Catálogo oficial incompleto (${target.size}/300).`);catalogCache=target;return target}const publicQuestion=(q:QuizQuestion)=>({id:q.id,difficulty:q.difficulty,baseXp:baseXp(q.difficulty),prompt:q.prompt,options:q.options,catalogVersion:QUIZ_CATALOG_VERSION});
async function resolveMember(request:Request,input:Record<string,unknown>,projectId:string,fireToken:string):Promise<MemberAuth>{const auth=request.headers.get("authorization")??"",bearer=auth.match(/^Bearer\s+(.+)$/i)?.[1]??"",provided=request.headers.get("apikey")??"",configured=Deno.env.get("SUPABASE_ANON_KEY")??"";let memberId="",phone="",firebase=false;const legacy=Boolean(configured)&&provided===configured&&bearer===configured,publishable=provided===CURRENT_PUBLISHABLE_KEY&&bearer===CURRENT_PUBLISHABLE_KEY;if(legacy||publishable){memberId=clean(input.memberId,200);phone=normalizePhone(input.phone);if(!memberId||phone.length<10||phone.length>11)throw new Error("Membro inválido.")}else{if(!bearer)throw new Error("Sessão ausente.");const v=await jwtVerify(bearer,FIREBASE_JWKS,{issuer:FIREBASE_ISSUER,audience:FIREBASE_PROJECT_ID,algorithms:["RS256"]});memberId=clean(v.payload.sub,200);firebase=true;if(!memberId)throw new Error("Sessão Firebase inválida.")}const access=docData(await getDocument(projectId,fireToken,"acessos_pendentes",memberId));if(!Object.keys(access).length)throw new Error("Cadastro do membro não encontrado.");if(firebase&&access.isApproved!==true&&access.isAdmin!==true)throw new Error("Acesso do membro ainda não aprovado.");if(!firebase&&normalizePhone(access.phone)!==phone)throw new Error("Identidade do membro não confere.");const user=docData(await getDocument(projectId,fireToken,"users",memberId));const unlocked=new Set([...stringList(access.unlockedBadgeIds),...stringList(user.unlockedBadgeIds)]);const entries=[...new Set([...(activityMap(access.badgeActivityIds).journey_xp_awards??[]),...(activityMap(user.badgeActivityIds).journey_xp_awards??[])])];return{memberId,xpUnlocked:[...unlocked].some(id=>LEVEL_8_PLUS.has(id)),legacyXp:entries.reduce((s,e)=>s+parseLegacyXp(e),0),legacyQuizIds:parseLegacyQuizIds(entries),firebaseAuth:firebase}}
Deno.serve(async(request)=>{if(request.method==="OPTIONS")return new Response("ok",{headers:cors});if(request.method!=="POST")return json({error:"Método não permitido."},405);try{const input=await request.json()as Record<string,unknown>;const action=clean(input.action,40);const sa=JSON.parse(Deno.env.get("FIREBASE_SERVICE_ACCOUNT_JSON")??"{}")as ServiceAccount;const projectId=sa.project_id||Deno.env.get("FIREBASE_PROJECT_ID")||FIREBASE_PROJECT_ID,fireToken=await googleToken(sa),member=await resolveMember(request,input,projectId,fireToken);if(["hint","answer","claim_mission"].includes(action)&&!member.firebaseAuth)return json({error:"Atualize o MIC Rhema e entre novamente para usar o Quiz com segurança."},401);const url=Deno.env.get("SUPABASE_URL")??"",role=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")??"";if(!url||!role)throw new Error("Backend XP não configurado.");const sb=createClient(url,role,{auth:{persistSession:false,autoRefreshToken:false}});const{data:ensured,error:ensureError}=await sb.rpc("xp_ensure_account",{p_member_id:member.memberId,p_legacy_xp:member.legacyXp});if(ensureError)throw ensureError;const account=Array.isArray(ensured)?ensured[0]:ensured;if(member.legacyQuizIds.length){const rows=member.legacyQuizIds.map(question_id=>({member_id:member.memberId,question_id}));const{error}=await sb.from("xp_legacy_quiz_receipts").upsert(rows,{onConflict:"member_id,question_id",ignoreDuplicates:true});if(error)throw error}const questions=await catalog();if(action==="status"||action==="next_question"){const d=difficultyFromToken(clean(input.difficulty,20))??"easy",ids=[...questions.values()].filter(q=>q.difficulty===d).map(q=>q.id);const[a,l]=await Promise.all([sb.from("xp_quiz_attempts").select("question_id").eq("member_id",member.memberId).in("question_id",ids),sb.from("xp_legacy_quiz_receipts").select("question_id").eq("member_id",member.memberId).in("question_id",ids)]);if(a.error)throw a.error;if(l.error)throw l.error;const answered=new Set([...(a.data??[]),...(l.data??[])].map((x:any)=>String(x.question_id))),next=ids.map(id=>questions.get(id)!).find(q=>!answered.has(q.id))??null;return json({ok:true,catalogVersion:QUIZ_CATALOG_VERSION,unlocked:member.xpUnlocked,difficulty:d,answered:answered.size,total:ids.length,question:next?publicQuestion(next):null})}const questionId=clean(input.questionId,200),question=questions.get(questionId);if(!question)return json({error:"Pergunta não pertence ao catálogo oficial do Quiz."},400);const{data:legacyReceipt,error:legacyError}=await sb.from("xp_legacy_quiz_receipts").select("question_id").eq("member_id",member.memberId).eq("question_id",question.id).maybeSingle();if(legacyError)throw legacyError;if(action==="hint"){if(legacyReceipt)return json({error:"Pergunta já concluída no progresso legado."},409);const requested=clean(input.hint,20),rank=requested==="easy"||requested==="easy_hint"?2:requested==="subtle"||requested==="subtle_hint"?1:0;if(!rank)return json({error:"Dica inválida."},400);const{data,error}=await sb.rpc("xp_record_quiz_hint",{p_member_id:member.memberId,p_question_id:question.id,p_hint_rank:rank});if(error)throw error;const effective=Number(data??rank),variant=effective>=2?"easy_hint":"subtle_hint";return json({ok:true,catalogVersion:QUIZ_CATALOG_VERSION,questionId:question.id,variant,hint:effective>=2?question.easyHint:question.hardHint,multiplier:effective>=2?.7:.9})}if(action==="answer"){const selected=Number(input.selectedOptionIndex);if(!Number.isInteger(selected)||selected<0||selected>3)return json({error:"Alternativa inválida."},400);const correct=selected===question.correctOptionIndex;if(legacyReceipt)return json({ok:true,catalogVersion:QUIZ_CATALOG_VERSION,unlocked:member.xpUnlocked,questionId:question.id,duplicate:true,legacy:true,granted:0,correct,selectedOptionIndex:selected,correctOptionIndex:question.correctOptionIndex,variant:"",reference:question.reference,explanation:question.explanation,account:{member_id:member.memberId,total_earned:Number(account?.total_earned??0),total_spent:Number(account?.total_spent??0),balance:Number(account?.balance??0)}});const{data,error}=await sb.rpc("xp_submit_quiz",{p_member_id:member.memberId,p_question_id:question.id,p_selected_option:selected,p_activity:question.activity,p_variant:"",p_correct:correct,p_amount:baseXp(question.difficulty),p_description:`Pergunta ${question.difficulty} correta`,p_auth_context:"firebase_member_v1"});if(error)throw error;const result=Array.isArray(data)?data[0]:data;const{data:attempt}=await sb.from("xp_quiz_attempts").select("selected_option,variant,correct").eq("member_id",member.memberId).eq("question_id",question.id).single();return json({ok:true,catalogVersion:QUIZ_CATALOG_VERSION,unlocked:member.xpUnlocked,questionId:question.id,duplicate:Boolean(result?.duplicate),granted:Number(result?.granted??0),correct:Boolean(attempt?.correct??result?.correct),selectedOptionIndex:Number(attempt?.selected_option??selected),correctOptionIndex:question.correctOptionIndex,variant:String(attempt?.variant??""),reference:question.reference,explanation:question.explanation,account:{member_id:member.memberId,total_earned:Number(result?.total_earned??0),total_spent:Number(result?.total_spent??0),balance:Number(result?.balance??0)}})}if(action==="claim_mission"){if(!member.xpUnlocked)return json({ok:true,unlocked:false,granted:0,reason:"xp_locked"});const missionId=clean(input.missionId,200);const activity:string|null=missionId.startsWith("easy_")?"journey_mission_easy":missionId.startsWith("medium_")?"journey_mission_medium":missionId.startsWith("hard_")?"journey_mission_hard":null;const amount=activity==="journey_mission_easy"?15:activity==="journey_mission_medium"?35:activity==="journey_mission_hard"?70:0;if(!activity||!amount)return json({error:"Missão da Jornada inválida."},400);const{data,error}=await sb.rpc("xp_award",{p_member_id:member.memberId,p_activity:activity,p_content_id:missionId,p_variant:"",p_receipt_id:`mission:${missionId}`,p_amount:amount,p_description:"Missão da Jornada concluída",p_daily_cap:0});if(error){const m=String(error.message??"");if(m.includes("ainda não foi concluída"))return json({ok:true,unlocked:true,granted:0,reason:"mission_incomplete"},409);throw error}const result=Array.isArray(data)?data[0]:data;return json({ok:true,unlocked:true,granted:Number(result?.granted??0),duplicate:Boolean(result?.duplicate),reason:"",account:{member_id:member.memberId,total_earned:Number(result?.total_earned??0),total_spent:Number(result?.total_spent??0),balance:Number(result?.balance??0)}})}return json({error:"Ação inválida."},400)}catch(error){console.error("xp-quiz failed",error);const message=error instanceof Error?error.message:"Falha no Quiz.",status=message.includes("Sessão")||message.includes("Identidade")?401:500;return json({error:message},status)}});